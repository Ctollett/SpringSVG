{
  "name": "ruun-monorepo",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "build": "pnpm --filter getruun build",
    "dev": "pnpm --parallel --filter '*' dev",
    "test": "pnpm --filter getruun test",
    "lint": "pnpm --filter '*' lint"
  }
}
