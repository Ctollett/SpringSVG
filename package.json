{
  "name": "ruun-monorepo",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "pnpm --filter ruun build",
    "dev": "pnpm --parallel --filter '*' dev",
    "test": "pnpm --filter ruun test",
    "lint": "pnpm --filter '*' lint"
  }
}
